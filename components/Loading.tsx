
export default function Loading() {
  return (
    <div
      style={{
        height: "100vh",
        background: "#000",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "#fff",
        fontSize: "30px",
        fontWeight: "bold",
      }}
    >
      Loading...
    </div>
  );
}

